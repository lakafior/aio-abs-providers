{
  "name": "aio-abs-providers",
  "version": "0.1.0",
  "private": true,
  "description": "Backbone and provider wrappers for AudiobookShelf",
  "main": "src/backbone/server.js",
  "scripts": {
    "start:backbone": "node src/backbone/server.js",
    "start:all": "npm run start:backbone",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "cheerio": "^1.0.0-rc.12",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "concurrently": "^8.0.0",
    "node-cache": "^5.1.2",
    "string-similarity": "^4.0.4",
    "ajv": "^8.12.0"
  }
}